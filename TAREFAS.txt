1=Colocar generate_password_hash e check_password_hash

2=Caso o usuário tente curtir ou comentar sem estar logado, criar modal para entrar

3=Para: Usuário ou senha incorreto, cor #FF0000
<div class="flash-container">
    {% with messages = get_flashed_messages(with_categories=true) %}
      {% if messages %}
        {% for category, message in messages %}
          <div class="alert alert-{{ category }}">
            {{ message }}
          </div>
        {% endfor %}
      {% endif %}
    {% endwith %}
</div>
.flash-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
    position: fixed;
    top: 20px;
    z-index: 1000; 
}

.alert {
    min-width: 300px;
    max-width: 500px;
    padding: 15px 20px;
    margin-bottom: 10px;
    border-radius: 8px;
    text-align: center;
    font-family: sans-serif;
    font-weight: bold;
    box-shadow: 0px 4px 6px rgba(0,0,0,0.1);
}

.alert-sucesso {
    background-color: #d4edda;
    color: #155724;
    border: 1px solid #c3e6cb;
}

.alert-erro {
    background-color: #f8d7da;
    color: #721c24;
    border: 1px solid #f5c6cb;
}


4=Identifique para as ações de edição e exclusão de comentários apenas ao próprio autor ou ao administrador.

5=Persistência, garantindo que as informações não se percam ao recarregar a página.